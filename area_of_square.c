#include <stdio.h>
int main(){
    // finding area of a square with side s

    int s;
    printf("Enter the side of square:\n");
    scanf("%d",&s);

    printf("Area of the Square is %d", s*s);



    return 0;
}