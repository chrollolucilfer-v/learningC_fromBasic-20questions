#include <stdio.h>
int main(){
    // area of circle = pi * r*r
    float r, pi =3.14;

    printf("Enter the radius of circle:\n");
    scanf("%f", &r);

    printf("Are of the circle is %f", pi* r*r);
    


    return 0;
}