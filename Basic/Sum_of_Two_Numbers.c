#include <stdio.h>
int main(){
    int a, b, sum;
    printf("Enter A:\n");
    scanf("%d", &a);
    printf("Enter B:  \n");
    scanf("%d", &b);
    printf("Sum of A and B is %d", a+b);
    return 0;
}






// What is Compilation?
// Compilation is the process of converting source code 
//written in a high-level programming language into 
//machine code that can be executed by a computer.

// A Computer program that translates C code into machine code
// hello.c ===> C Compiler ===> a.exe(windows) or a.out(linux) ===> Machine Code

// if erros compilation fails

