#include<stdio.h>
int main(){
    // Dynamic Memory Allocation in C
    // a way to allocate memory to a data structure during the runtime
    // we need some functions to allocate and free memory dynamically


    // functions for DMA
    // malloc() - allocates memory of a specified size and returns a pointer to the first byte of the allocated memory block.
    // calloc() - allocates memory for an array of elements and initializes all bytes to zero.
    // realloc() - changes the size of the memory block pointed to by the pointer.
    // free() - deallocates the memory block pointed to by the pointer.


    










    return 0;
}